apiVersion: v1
kind: Service
metadata:
  name: {{ include "safe-settings.fullname" . }}
  labels:
    {{- include "safe-settings.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "safe-settings.selectorLabels" . | nindent 4 }}
